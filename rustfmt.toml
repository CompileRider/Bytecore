# rustfmt.toml

# Use the 2024 edition style guidelines.
edition = "2024"

# Try to fit code on a single line.
# "Max" is a conservative choice that prioritizes readability.
use_small_heuristics = "Max"

# Use Unix-style line endings (LF).
newline_style = "Unix"
